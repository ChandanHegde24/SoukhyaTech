import React from 'react';

const CTA = () => {
  return (
    <section className="cta">
      <div className="cta-container">
        <h2 className="cta-title">Ready to Build Smart Connected Solutions?</h2>
        <button className="cta-button">Contact Us Today</button>
      </div>
    </section>
  );
};

export default CTA;
